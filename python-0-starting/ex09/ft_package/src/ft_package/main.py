def count_in_list(items, target):
    """
    Counts the occurrences of a target element in a list.

    Args:
        items (list): The list to search.
        target: The element to count in the list.

    Returns:
        int: The number of occurrences of the target element.
    """
    return items.count(target)
